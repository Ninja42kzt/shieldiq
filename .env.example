
# --- CodeGuard scan module (added) ---
# Optional: GitHub token for cloning private repos during IT scans.
# Single-token/single-tenant only — see chat notes on multi-tenant OAuth.
GITHUB_TOKEN=
CODEGUARD_TEMP_DIR=./services/codeguard/temp
CODEGUARD_MAX_REPO_SIZE_MB=100
CODEGUARD_CLONE_TIMEOUT_MS=300000
